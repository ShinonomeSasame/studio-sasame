# CLAUDE.md

このリポジトリで作業する際の方針・規約をまとめたもの。具体的に何を作るプロジェクトかは `docs/100_企画/110_コンセプト/111_コンセプト.md` を参照すること。

## 技術方針

- ビルドレスを基本とする。バンドラ(Vite/webpackなど)やnpm依存は導入しない
- JavaScriptはネイティブESモジュール(`<script type="module">` + `import`/`export`)でファイルを分割する
- ローカル動作確認は `file://` ではなく簡易HTTPサーバー経由で行う(ESモジュールがCORSでブロックされるため)
- 動作確認の対象環境は最新版のChrome(PC/モバイル)を基本とする。他ブラウザ対応が必要な場合はプロジェクトごとに `docs/100_企画` の成果物に明記する

## ディレクトリ構成

- `docs/` は工程ごとに `100_企画` `200_設計` `300_開発` `400_検証` の番号付きフォルダで構成する
- 各工程フォルダの直下は `XY0_カテゴリ名/XY1_ファイル名.拡張子` の形式でサブフォルダ・ファイルを持つ
- 雛形を別途 `_templates/` のような場所に用意することはしない。各工程フォルダに直接、完成形のファイル名で成果物を置く
- `assets/` は画像・フォントなど静的アセット置き場。命名規則は必要になった時点で追記する

## 開発フロー

企画 → 設計 → 開発 → 検証 の4工程で進める。ただし厳密な一方通行(ウォーターフォール)にはしない:

- 企画〜設計の間に小さなプロトタイプを作り、コア体験が面白いか確認してから設計を確定する
- 検証で見つかった問題は設計に戻って修正してよい。手戻りは前提として許容する

Webサイト開発など探索的要素が少ない場合は、一方通行で進めても問題ない。

## 意思決定ログの運用ルール

非自明な技術判断(ライブラリ選定、実装方針、他の選択肢を却下した理由など)をしたら、`docs/300_開発/310_決定メモ/311_決定メモ.md` に追記する。git commitメッセージは「何を変更したか」しか残らないため、「なぜその判断をしたか」はこのファイルで補う。

フォーマット例:

```
## 2026-08-10 モジュール方式
ESモジュール採用。理由: グローバル汚染回避、import順の手動管理が不要。
検討した代替: 素のscriptタグ(却下: 大規模化時の名前衝突リスク)
```

## タスク管理

作業項目の管理・進捗は GitHub Issues で行う。`docs/` 配下には日々変動するタスクリストを置かない。
