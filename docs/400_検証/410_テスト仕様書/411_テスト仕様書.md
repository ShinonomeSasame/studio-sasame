# テスト仕様書

## テストケース

### 2026-08-14 Epic #1/#2/#3・Feature #10〜#15 検証(HOMEページ基盤)

検証方法: `python -m http.server` で`index.html`をHTTP経由で配信し、CDP(Chrome DevTools Protocol)経由のヘッドレスChromeで実際のクリック座標を用いた操作確認、および`getComputedStyle`/`document.elementFromPoint`等によるDOM/CSS検証を実施。

| No. | 項目 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| *(例: 1)* | *(例: ステージクリア)* | *(例: ゴールまでブロックを移動させる)* | *(例: クリア演出が表示される)* | |
| 2 | 共通ヘッダー表示(#10) | HTTP経由で`index.html`を開く | ヘッダーにロゴ「スタジオささめ」とハンバーガーボタンが表示される | OK(HOMEページのみ。ABOUT等未実装ページは今後再確認) |
| 3 | ロゴのHOME遷移(#1,#10) | ロゴをクリックする | `index.html`(HOME)へのリンクになっている | OK(`href="index.html"`を確認) |
| 4 | ハンバーガーボタン表示(#10) | `index.html`を開く | ヘッダー右端にハンバーガーボタンが表示される | OK |
| 5 | ×アイコンへの切り替え(#11) | ハンバーガーボタンをクリックする | ボタンの3本線が回転・不透明度変化しXの形になる(`getComputedStyle`でbar1/bar3が`rotate(±45deg)`、bar2が`opacity:0`) | OK |
| 6 | メニューのスライドイン(#11) | ハンバーガーボタンをクリックする | `.site-nav`に`is-open`が付与され`transform: translateX(0)`となり、HOME/ABOUT/GALLERY/CONTACT/プライバシーポリシーの5リンクが表示される | OK |
| 7 | メニューリンクでの遷移(#1,#11) | メニュー内「ABOUT」リンクをクリックする | `location.href`が`about.html`に変わる(ページ自体は未実装のため現状404だが、既知の状態として許容) | OK(`about.html`遷移を確認。実ファイルは404、既知) |
| 8 | ×ボタンでのスライドアウト(実クリック)(#11) | メニューを開いた状態で、ハンバーガーボタンと同じ画面座標を実際のマウス座標でクリックする(`Input.dispatchMouseEvent`、モバイル幅400px/デスクトップ幅1280pxの両方で実施) | `.hamburger-btn`/`.site-nav`から`is-open`が外れ、メニューが右端にスライドアウトする | **NG**。`document.elementFromPoint(ボタン中心座標)`が`.hamburger-btn`ではなく`.site-nav`を返し、実クリックがメニュー背景に吸収されて反応しない。JSから`.click()`を直接呼んだ場合のみ正しく閉じる。原因: `.site-header`(z-index:100)が独自スタッキングコンテキストを作り、ボタンの`z-index:200`はその内部でのみ有効なため、兄弟要素`.site-nav`(z-index:150)より背面に描画される。→ Issue #11 を差し戻し |
| 9 | カラーパレットのCSS変数(#2,#12) | `getComputedStyle(document.documentElement)`で`--color-bg`/`--color-primary`/`--color-text`等を取得 | 決定メモの色コード(`#faf9f6`/`#165e83`/`#333333`等)と一致する | OK |
| 10 | フォント適用(#2,#12) | `document.fonts`および`getComputedStyle(body)`/`h2`のfont-familyを確認 | `TanukiMagic`が`@font-face`で読み込まれ`status:"loaded"`、body/見出しに適用されている | OK(HOMEページのみ。他ページは未実装のため今後再確認) |
| 11 | favicon設定(#2,#13) | `<head>`の`link[rel="icon"]`と`assets/images/favicon/favicon.png`のHTTPレスポンスを確認 | linkタグが存在し、favicon.pngが200/`image/png`で取得できる | OK(HOMEページのみ。他ページは未実装のため今後再確認) |
| 12 | お知らせ欄の表示位置(#3,#14) | `main > section`の並び順を確認 | `.announcements`が`.latest-game`より先(HOMEページ最上部)に配置されている | OK |
| 13 | お知らせ内容のHTML直書き(#14) | `index.html`のソースを確認 | `<li><time>...</time><p>...</p></li>`がHTMLに直書きされている | OK |
| 14 | 最新ゲームカードの表示(#3,#15) | 最新ゲーム欄のサムネイル・タイトルを確認 | `card.css`のカード形式(サムネイル+タイトル+説明文)でGALLERYと共用予定のスタイルが適用されている | OK |
| 15 | 最新ゲームカードのリンク遷移(#15) | 最新ゲームカードをクリックする | 該当ゲームのPLAYページへ遷移する(URLが変化する) | **NG**。`href="#"`のプレースホルダーのままで、クリックしても`location.href`が変化しない(ページ内遷移が発生しない)。→ Issue #15 を差し戻し |

## 不備・差し戻し一覧

| Issue | 内容 | 対応 |
|---|---|---|
| #11 | メニュー表示中、ハンバーガーボタン(×アイコン)が`.site-nav`の背面(z-index)に隠れて実際のクリックでは反応しない。JS経由の`.click()`は動作するが、実際のマウス/タップ操作では×ボタンを押せずメニューを閉じられない。 | 差し戻し(1回目)。Issueをreopenし、タスク概要に原因・修正方針案を追記、コメントに再現手順を記載。 |
| #15 | 最新ゲームカードのリンク先が`href="#"`で、クリックしても実際にはページ遷移が発生しない。 | 差し戻し(1回目)。Issueをreopenし、タスク概要に修正方針案(ハンバーガーメニューのリンクと同様に暫定パスへ変更)を追記、コメントに再現手順を記載。 |

## 今後の再検証が必要な項目(現時点では未実装ページに依存するため不備扱いとしない)

- Issue #1/#10「全ページ(ABOUT/GALLERY/CONTACT/プライバシーポリシー/PLAY)で同じヘッダーが表示される」: 現時点ではHOMEページのみ実装されているため未検証。各ページ実装時(Issue #16/#17/#19/#20/#22)に再確認する。
- Issue #2/#12「全ページでたぬき油性マジック体が適用されている」、Issue #2/#13「全ページの`<head>`にfaviconのlinkタグ」: 同上の理由で他ページ実装時に再確認する。
- ハンバーガーメニューの`about.html`/`gallery.html`/`contact.html`/`privacy-policy.html`へのリンクは、対象ページが未実装のため現状404になる。リンク先パス自体は決定メモ(`311_決定メモ.md`)の設計方針(ルート直下フラット構成)と一致しており、意図通りであることを確認済み。

## リリース前チェックリスト

- [ ] 素材ライセンス確認
- [ ] リンク切れ・404チェック
- [ ] レスポンシブ/主要ブラウザ動作確認
