# テスト仕様書

## テストケース

### 2026-08-14 Epic #1/#2/#3・Feature #10〜#15 検証(HOMEページ基盤)

検証方法: `python -m http.server` で`index.html`をHTTP経由で配信し、CDP(Chrome DevTools Protocol)経由のヘッドレスChromeで実際のクリック座標を用いた操作確認、および`getComputedStyle`/`document.elementFromPoint`等によるDOM/CSS検証を実施。

| No. | 項目 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| *(例: 1)* | *(例: ステージクリア)* | *(例: ゴールまでブロックを移動させる)* | *(例: クリア演出が表示される)* | |
| 2 | 共通ヘッダー表示(#10) | HTTP経由で`index.html`を開く | ヘッダーにロゴ「スタジオささめ」とハンバーガーボタンが表示される | OK(HOMEページのみ。ABOUT等未実装ページは今後再確認) |
| 3 | ロゴのHOME遷移(#1,#10) | ロゴをクリックする | `index.html`(HOME)へのリンクになっている | OK(`href="index.html"`を確認) |
| 4 | ハンバーガーボタン表示(#10) | `index.html`を開く | ヘッダー右端にハンバーガーボタンが表示される | OK |
| 5 | ×アイコンへの切り替え(#11) | ハンバーガーボタンをクリックする | ボタンの3本線が回転・不透明度変化しXの形になる(`getComputedStyle`でbar1/bar3が`rotate(±45deg)`、bar2が`opacity:0`) | OK |
| 6 | メニューのスライドイン(#11) | ハンバーガーボタンをクリックする | `.site-nav`に`is-open`が付与され`transform: translateX(0)`となり、HOME/ABOUT/GALLERY/CONTACT/プライバシーポリシーの5リンクが表示される | OK |
| 7 | メニューリンクでの遷移(#1,#11) | メニュー内「ABOUT」リンクをクリックする | `location.href`が`about.html`に変わる(ページ自体は未実装のため現状404だが、既知の状態として許容) | OK(`about.html`遷移を確認。実ファイルは404、既知) |
| 8 | ×ボタンでのスライドアウト(実クリック)(#11) | メニューを開いた状態で、ハンバーガーボタンと同じ画面座標を実際のマウス座標でクリックする(`Input.dispatchMouseEvent`、モバイル幅400px/デスクトップ幅1280pxの両方で実施) | `.hamburger-btn`/`.site-nav`から`is-open`が外れ、メニューが右端にスライドアウトする | **NG**。`document.elementFromPoint(ボタン中心座標)`が`.hamburger-btn`ではなく`.site-nav`を返し、実クリックがメニュー背景に吸収されて反応しない。JSから`.click()`を直接呼んだ場合のみ正しく閉じる。原因: `.site-header`(z-index:100)が独自スタッキングコンテキストを作り、ボタンの`z-index:200`はその内部でのみ有効なため、兄弟要素`.site-nav`(z-index:150)より背面に描画される。→ Issue #11 を差し戻し |
| 9 | カラーパレットのCSS変数(#2,#12) | `getComputedStyle(document.documentElement)`で`--color-bg`/`--color-primary`/`--color-text`等を取得 | 決定メモの色コード(`#faf9f6`/`#165e83`/`#333333`等)と一致する | OK |
| 10 | フォント適用(#2,#12) | `document.fonts`および`getComputedStyle(body)`/`h2`のfont-familyを確認 | `TanukiMagic`が`@font-face`で読み込まれ`status:"loaded"`、body/見出しに適用されている | OK(HOMEページのみ。他ページは未実装のため今後再確認) |
| 11 | favicon設定(#2,#13) | `<head>`の`link[rel="icon"]`と`assets/images/favicon/favicon.png`のHTTPレスポンスを確認 | linkタグが存在し、favicon.pngが200/`image/png`で取得できる | OK(HOMEページのみ。他ページは未実装のため今後再確認) |
| 12 | お知らせ欄の表示位置(#3,#14) | `main > section`の並び順を確認 | `.announcements`が`.latest-game`より先(HOMEページ最上部)に配置されている | OK |
| 13 | お知らせ内容のHTML直書き(#14) | `index.html`のソースを確認 | `<li><time>...</time><p>...</p></li>`がHTMLに直書きされている | OK |
| 14 | 最新ゲームカードの表示(#3,#15) | 最新ゲーム欄のサムネイル・タイトルを確認 | `card.css`のカード形式(サムネイル+タイトル+説明文)でGALLERYと共用予定のスタイルが適用されている | OK |
| 15 | 最新ゲームカードのリンク遷移(#15) | 最新ゲームカードをクリックする | 該当ゲームのPLAYページへ遷移する(URLが変化する) | **NG**。`href="#"`のプレースホルダーのままで、クリックしても`location.href`が変化しない(ページ内遷移が発生しない)。→ Issue #15 を差し戻し |

## 不備・差し戻し一覧

| Issue | 内容 | 対応 |
|---|---|---|
| #11 | メニュー表示中、ハンバーガーボタン(×アイコン)が`.site-nav`の背面(z-index)に隠れて実際のクリックでは反応しない。JS経由の`.click()`は動作するが、実際のマウス/タップ操作では×ボタンを押せずメニューを閉じられない。 | 差し戻し(1回目)。Issueをreopenし、タスク概要に原因・修正方針案を追記、コメントに再現手順を記載。 |
| #15 | 最新ゲームカードのリンク先が`href="#"`で、クリックしても実際にはページ遷移が発生しない。 | 差し戻し(1回目)。Issueをreopenし、タスク概要に修正方針案(ハンバーガーメニューのリンクと同様に暫定パスへ変更)を追記、コメントに再現手順を記載。 |
| #18 | GALLERYカードのサムネイルが1.91:1のアスペクト比にならない(`<img width height>`属性由来のプレゼンテーショナルヒントが`aspect-ratio`より優先され、実測446×630pxの縦長になる。GALLERY/HOME共通の`card.css`が原因、PLAYページの`#game-stage`は問題なし)。また`.game-card__title`に行数上限のCSSがなく、長いタイトルでカード高さが無制限に伸びる(説明文は`-webkit-line-clamp:3`で制限あり、タイトルは未制限)。 | 差し戻し(1回目)。Issueをreopenし、タスク概要に原因・修正方針案を追記、コメントに再現手順を記載。親Epic #5もあわせて再オープン。 |

## 今後の再検証が必要な項目(現時点では未実装ページに依存するため不備扱いとしない)

- ハンバーガーメニューの`contact.html`/`privacy-policy.html`へのリンクは、対象ページが未実装のため現状404になる。リンク先パス自体は決定メモ(`311_決定メモ.md`)の設計方針(ルート直下フラット構成)と一致しており、意図通りであることを確認済み(Issue #7/#8/#20〜#22で実装予定)。
- Issue #9/#23/#24(OGPメタタグ)は未実装。ABOUT/GALLERY/PLAYページとも`<head>`に`og:*`/`twitter:*`タグはまだ無く、今回は検証対象外(既知の未実装スコープ)。
- PLAYページへのPhaserゲーム本体の組み込みは別タスク(Issue #19の受入条件は「枠のみ」のため対象外)。
- Issue #18の修正(サムネイルのアスペクト比・タイトル行数上限)が反映された際、HOMEページ(`index.html`)の最新ゲームカードの見た目も合わせて再確認する(共通の`card.css`を使用しているため)。

### 2026-08-15 Epic #4/#5/#6・Feature #16/#17/#18/#19 検証(ABOUT/GALLERY/PLAYページ)

検証方法: `python -m http.server`で`about.html`/`gallery.html`/`play.html`をHTTP経由で配信し、`puppeteer-core`(既存のローカルChromeを`executablePath`で指定)経由のヘッドレスChromeで実際のクリック座標を用いた操作確認・ページ遷移確認、および`getComputedStyle`/`getBoundingClientRect`/`document.elementFromPoint`等によるDOM/CSS実測を実施。デスクトップ幅(1280px)とモバイル幅(375px)の両方で確認。

| No. | 項目 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| 16 | 共通ヘッダー表示(#1,#10,#16,#19) | HTTP経由で`about.html`/`gallery.html`/`play.html`を開く | いずれも`.site-header`にロゴ「スタジオささめ」とハンバーガーボタンが表示される | OK |
| 17 | フォント適用(#2,#12) | `about.html`/`gallery.html`/`play.html`で`document.fonts.ready`後に`TanukiMagic`の読み込み状態とbodyの`font-family`を確認 | `TanukiMagic`が`status:"loaded"`、bodyに適用されている | OK(3ページとも確認) |
| 18 | favicon設定(#2,#13) | `about.html`/`gallery.html`/`play.html`の`<head>`と`assets/images/favicon/favicon.png`のHTTPレスポンスを確認 | linkタグが存在し、favicon.pngが200/`image/png`で取得できる | OK(3ページとも確認) |
| 19 | ハンバーガーボタンの実クリック(#11の再発確認、#16/#17/#19の共通ヘッダー) | `about.html`(デスクトップ幅)・`gallery.html`(モバイル幅375px)でハンバーガーボタンを実クリックしメニューを開く。`document.elementFromPoint`でボタン中心座標の要素を確認 | `.site-nav`に`is-open`が付与されメニューが開き、開いた状態でもボタン中心座標の要素が`.hamburger-btn`自身(または内包要素)である(以前z-indexの不具合で発生していたクリック不能状態が再発していない) | OK(`about.html`デスクトップ幅・`gallery.html`モバイル幅375pxともに`btnClickableWhileOpen: true`。Issue #11の修正が新規ページにも反映されていることを確認) |
| 20 | メニュー内リンクの実クリック(#16/#17/#19) | メニューを開いた状態で`#site-nav a[href="gallery.html"]`の中心座標に対応する要素を確認 | 座標上の要素がリンク自身(または内包要素)である(ナビ背景に埋もれていない) | OK |
| 21 | 作者プロフィール表示(#4,#16) | `about.html`の`.author-profile`を確認 | 氏名・アイコン・紹介文が表示される | OK(プレースホルダー文言だが表示は正常。文言・アイコンは未確定のため既知の事項) |
| 22 | サイト概要表示(#4,#16) | `about.html`の`.site-overview`を確認 | サイト概要の文章が表示される | OK |
| 23 | SNSリンク表示・属性(#4,#16) | `about.html`の`.sns-links__link`の`href`/`target`/`rel`を確認 | リンクが表示され、`target="_blank"`・`rel="noopener noreferrer"`が付与されている | OK(URLはプレースホルダー`https://x.com/example_placeholder`。実URLは未確定のため既知の事項) |
| 24 | GALLERYカードのレスポンシブ列数(#5,#17) | `gallery.html`をデスクトップ幅1280px/モバイル幅375pxで開き、`.gallery__list`の`grid-template-columns`の列数を確認 | デスクトップで2列、モバイルで1列 | OK(デスクトップ`colCount:2`、モバイル`colCount:1`) |
| 25 | GALLERYカードの構成要素(#17) | `gallery.html`の`.game-card`を確認 | サムネイル・タイトル・説明文が1カード内に存在する | OK |
| 26 | GALLERYカードのPLAYページ遷移(#5,#6,#17) | `gallery.html`の`.game-card`(`href="play.html"`)を実クリックし遷移後のURLを確認 | `play.html`へページ遷移する | OK(`page.url()`が`http://localhost:8765/play.html`に変化することを確認) |
| 27 | GALLERYカードの新着順(#17) | `gallery.html`のHTML構造・コメントを確認 | 新着順(公開日が新しい順)に記述する運用ルールがコメントで明記されている | OK(現状プレースホルダー1件のみのため並び順自体の目視比較はできないが、運用ルールの明記を確認。件数が増えた際に再確認が必要) |
| 28 | GALLERYカード共通スタイル(角丸/影/余白)(#18) | `gallery.html`の`.game-card`の`getComputedStyle`で`border-radius`/`border`/`padding`/`box-shadow`(hover時)を確認 | 角丸・枠線・余白がカード間で共通化されている | OK(`card.css`で`border-radius:12px`・`border:1px solid`・`padding:1rem`等が共通定義されていることを確認) |
| 29 | GALLERYカードのサムネイルアスペクト比(#18) | `gallery.html`の`.game-card__thumbnail`の実測サイズ(`getBoundingClientRect`)から幅/高さ比を算出 | 1.91:1(横長)で表示される | **NG**。実測446×630px(縦長、比率0.71)。`<img width="1200" height="630">`属性由来のプレゼンテーショナルヒント`height:630px`が`aspect-ratio:1.91/1`(CSS)より優先され、意図した横長比率にならない。`height`属性削除疑いを`img`から`removeAttribute("height")`で検証したところ233.5px(比率1.91)に復帰することを確認、原因を特定。→ Issue #18を差し戻し |
| 30 | GALLERYカードのタイトル・説明文の行数上限統一(#18) | `.game-card__title`/`.game-card__description`の`getComputedStyle`で`-webkit-line-clamp`を確認。長いタイトル文字列に差し替えてカード高さの変化を確認 | タイトル・説明文とも行数上限が設定されており、長文でもカード高さが一定範囲に収まる | **NG**。`.game-card__description`は`-webkit-line-clamp:3`が設定されているが、`.game-card__title`には行数制限のCSSが無い(`lineClamp:"none"`)。長いタイトル文字列に差し替えるとカード高さが約754px→約813pxまで無制限に伸びることを確認。→ Issue #18を差し戻し(No.29と合わせて1件の差し戻しとして対応) |
| 31 | PLAYページの共通ヘッダー・ゲームエリア枠(#6,#19) | `play.html`を開き`.site-header`と`#game-stage`の表示を確認 | 共通ヘッダーとゲーム描画エリアの枠(`#game-stage`)が表示される | OK |
| 32 | PLAYページのゲームエリアのアスペクト比(#19関連の実装品質確認) | `play.html`の`#game-stage`の実測サイズから幅/高さ比を算出 | 1.91:1で表示される(GALLERYカードと同じ比率で、`og:image`にも流用できる想定) | OK(実測比率1.910。`play.css`が`#game-stage`に`height:100%`を明示指定しているため、GALLERYカードと異なりNo.29の不具合は発生していない) |
| 33 | PLAYページのタイトル/説明文のHTML直書き(#19) | `play.html`の`.play-page__title`/`.play-page__description`のテキストを確認 | 個別ゲームのタイトル・説明文がHTMLに直書きされている | OK |
| 34 | PLAYページのCSSクラス名/id衝突確認(#19) | `src/css/base.css`/`header.css`/`card.css`内に`game-stage`という語が使われていないことを確認 | `#game-stage`配下が個別ゲーム専用スコープとして衝突しない設計になっている | OK |
| 35 | コンソールエラー確認 | `about.html`/`gallery.html`/`play.html`を開いた際の`console.error`/`pageerror`を収集 | JSエラーが発生しない | OK(3ページともエラー0件) |

## リリース前チェックリスト

- [ ] 素材ライセンス確認
- [ ] リンク切れ・404チェック
- [ ] レスポンシブ/主要ブラウザ動作確認
